<%- include("partials/header") -%>

<% posts.forEach((post) => {%>
    <h1><%= post.title %></h1>
    <p class="inline">
        <%= post.content %>
    </p>
<% }); %>

<%- include("partials/footer") -%>